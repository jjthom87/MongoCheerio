<h1 class="text-center">Welcome to the Scraper</h1>

<div class = "row">
	<div class="col-xs-6 col-sm-3">
	</div>
	<div class="col-xs-6 col-sm-2 postDiv">
		<h2 id = "commentTag">Click on Album to Comment</h2>

{{!-- 		<form method="POST" action="/comment" class="button-size">
			<textarea type="text" name="comment"></textarea>
			<br>
			<button class="btn btn-warning type"="submit">Submit comment</button>
		</form> --}}
{{!-- 		<input id = "nameInput" type="text" name = "name" />
		<input id = "commInput" type="text" name = "comment" /> --}}
		<div id = "commentDiv"></div>
	</div>
	<div class="col-xs-6 col-sm-2">
		<h2>Check out this Album</h2>
{{!-- 		<h4>Please click the title to Comment</h4> --}}
		<div id="apiResults"></div>
		<button id="prevAlbum" class="btn btn-warning">Previous Album</button>
		<button id="nextAlbum" class="btn btn-warning">Next Album</button>
	</div>
	<div class="col-xs-6 col-sm-2 commentDiv">
		<h2>Review Comments</h2>
{{!-- 		{{#each comments}}
			{{#if this.status}}
		<p class="alert alert-danger comments" style="width: 40%">
				{{this.comment}}
		</p>
			{{/if}}
		{{/each}} --}}
		
		<div id="reviewDiv"></div>
		<form action="/update?_method=PUT" method="POST">
			<input type="hidden" name="status" value="{{this.status}}">
			<button type="submit" class="btn btn-warning">Delete Comment</button>
		</form>
	</div>
	<div class="col-xs-6 col-sm-3">
	</div>
</div>